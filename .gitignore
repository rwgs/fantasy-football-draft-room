node_modules/
dist/
server/data/cache/
.env
.env.local
.env.*.local

# Names real Sleeper leagues, so it never goes in the repo. See
# client/fixtures.example.json.
client/fixtures.local.json

# Scratch work against the Yahoo API: a throwaway probe and what it dumps.
tools/

# Playwright MCP scratch output from browser checks
.playwright-mcp/

# What `npm run shots` photographs. Evidence for one change, not an asset.
client/shots/

# Editor and OS noise
.vs/
.DS_Store
*.local
